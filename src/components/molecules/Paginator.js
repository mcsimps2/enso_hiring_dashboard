import React from 'react';

// You may write your pagination component here
// Feel free to setup the component according to your own coding style
// (e.g. subclasses Component vs. using React hooks, declaring prop types, etc...)
const Paginator = (props) => {
    return null;
};

export default Paginator;
